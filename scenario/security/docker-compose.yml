version: '3.11'

networks:
  custom:
    name: alluxio-network
    external: true

volumes:
  share:
    name: alluxio-share
    external: true

services:
  kdc:
    extends:
      file: ../../component/kerberos.yml
      service: kdc
  namenode:
    extends:
      file: ../../component/hadoop.yml
      service: namenode
  datanode:
    extends:
      file: ../../component/hadoop.yml
      service: datanode